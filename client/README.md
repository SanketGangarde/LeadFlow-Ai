# LeadFlow AI — Frontend

React + Vite frontend for the LeadFlow AI application.

## Running Locally

```bash
npm install
npm run dev
```

Opens at `http://localhost:5173`.

## Environment Variables

Create a `.env` file:

```env
VITE_API_URL=http://localhost:5000/api
```
